JS-PCB By Chris Hinsley 2015
===========================

JavaScript PCB router and solver.

Format of .pcb input data is:

[
[width, height, depth],
[track_radius, via_radius, track_gap,
 	[[terminal_radius, terminal_gap, [x, y, z], [[x, y], ...]), ...]...],
[track_radius, via_radius, track_gap,
 	[[terminal_radius, terminal_gap, [x, y, z], [[x, y], ...]), ...]...],
[track_radius, via_radius, track_gap,
 	[[terminal_radius, terminal_gap, [x, y, z], [[x, y], ...]), ...]...],
[track_radius, via_radius, track_gap,
 	[[terminal_radius, terminal_gap, [x, y, z], [[x, y], ...]), ...]...],
]

Format of the viewer input is similar but has the track paths appended:

[
[width, height, depth],
[track_radius, via_radius, track_gap,
 	[[terminal_radius, terminal_gap, [x, y, z], [[x, y], ...]), ...]...],
 	[[[x, y, z], ...], [[x, y, z], ...], [[x, y, z], ...], ...],
]
