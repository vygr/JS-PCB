JS-PCB By Chris Hinsley 2015
============================

JavaScript PCB router and solver.

Format of PCB input data is:

NODE_2D		[x, y]
NODE_3D		[x, y, z]
SHAPE		[NODE_2D, NODE_2D, ...]
PATH		[NODE_3D, NODE_3D, ...]
TERMINAL	[terminal_radius, terminal_gap, NODE_3D, SHAPE]
PATHS		[PATH, PATH, ...]
PCB			{dims: [width, height, depth],
			tracks:
			[[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...]],
			[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...]],
			[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...]]]}

Format of the viewer input is similar to the pcb format but has the solved track paths appended:

{dims: [width, height, depth],
tracks:
[[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...], PATHS],
[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...], PATHS],
[track_radius, via_radius, track_gap, [TERMINAL, TERMINAL, ...], PATHS]]}
